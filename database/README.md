# Database

As migrations EF Core são mantidas junto da API em `backend/src/FamilyPlus.Api/Migrations`, pois pertencem ao `FinanceDbContext`. Esta pasta documenta a convenção e reserva espaço para scripts de diagnóstico ou migrações operacionais que venham a ser necessários.
